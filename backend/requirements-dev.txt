# Development-only tooling (tests + packaging). Install into the backend venv:
#     .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pyinstaller>=6.0
